{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "quest.schema.json",
  "title": "PolyQuest quest resource",
  "description": "A quest body or an invocation of a quest template. Store under data/<namespace>/quests/**/*.json.",
  "oneOf": [
    { "$ref": "polyquest.schema.json#/$defs/questBody" },
    { "$ref": "#/$defs/templateInvocation" }
  ],
  "$defs": {
    "templateInvocation": {
      "title": "PolyQuest template invocation",
      "description": "Arguments replace ${name} markers. Other fields override the expanded prototype at the top level.",
      "type": "object",
      "required": ["template"],
      "properties": {
        "template": { "$ref": "polyquest.schema.json#/$defs/identifier" },
        "arguments": {
          "type": "object",
          "description": "Argument values may be any JSON value. Whole-value markers preserve the value's JSON type.",
          "default": {},
          "additionalProperties": true
        },
        "availability": { "enum": ["daily", "unique"] },
        "difficulty": { "enum": ["easy", "medium", "hard"] },
        "title": { "type": "string", "pattern": "\\S" },
        "description": {
          "type": "array",
          "items": { "type": "string" }
        },
        "condition": { "$ref": "polyquest.schema.json#/$defs/condition" },
        "rewards": { "$ref": "polyquest.schema.json#/$defs/rewardPlan" }
      },
      "additionalProperties": false
    }
  }
}
