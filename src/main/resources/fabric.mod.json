{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "A server-side, data-driven quest engine for Fabric.",
  "authors": [
    "RenardElectric"
  ],
  "contact": {},
  "license": "MIT",
  "icon": "assets/polyquest/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "polycube.polyquest.PolyQuest"
    ]
  },
  "mixins": [
    "polyquest.mixins.json"
  ],
  "depends": {
    "java": ">=${java_version}",
    "minecraft": "${minecraft_version}",
    "fabricloader": ">=${loader_version}",
    "fabric-api": ">=${fabric_api_version}"
  },
  "accessWidener": "polyquest.classtweaker"
}
